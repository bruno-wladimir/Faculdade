<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AutenticacaO.Views.MainPage"
             Title="MainPage">
  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
    <StackLayout HorizontalOptions="Center">
      <Label Text="User"></Label>
      <Entry  Text="{Binding User}"/>
      <Label Text="Senha"></Label>
      <Entry  Text="{Binding Senha}"/>
      <Button Text="Login" Command="{Binding CommandLogar}"/>

      <Label Text="Fazer Login Com Facebook"></Label>
      <Button Text="Login" Command="{Binding CommandLogarFacebook}"/>


      <Label Text="{Binding Title}" />
    </StackLayout>
  </StackLayout>
</ContentPage>